package UnitTests;
/*
import static org.junit.Assert.*;

import java.awt.Color;

import org.junit.Test;

import primitives.Point3D;
import primitives.Vector;
import elements.PointLight;
*/
public class PointLightTest {
/*
	@Test
	public void getIntensityTest() {
		PointLight pl = new PointLight(new Color(100,0,255),new Point3D(0,0,0),0.8,0.1,0.2);
		Color intensity = pl.getIntensity(new Point3D(3,4,5));
		assertEquals(intensity.getRed(),17);
		assertEquals(intensity.getGreen(),0);
		assertEquals(intensity.getBlue(),45);
	}
	
	@Test
	public void getLTest() {
		PointLight pl = new PointLight(new Color(100,0,255),new Point3D(4,0,2),0.8,0.1,0.2);
		Vector L = pl.getL(new Point3D(3,5,4));
		assertEquals(L,new Vector(-1,5,2));
	}
*/
}
