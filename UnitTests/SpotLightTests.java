package UnitTests;
/*
import static org.junit.Assert.*;

import java.awt.Color;

import org.junit.Test;

import primitives.*;
import elements.SpotLight;
*/
public class SpotLightTests {
/*
	@Test
	public void getIntensityTest() {
		SpotLight s = new SpotLight(new Color(100,0,255),new Point3D(0,0,0),0.8,0.1,0.2,new Vector(1,2,3));
		Color intensity =  s.getIntensity(new Point3D(3,4,5));
		assertEquals(intensity.getRed(),17);
		assertEquals(intensity.getGreen(),0);
		assertEquals(intensity.getBlue(),44);
	}
*/
}
